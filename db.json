const fs = require('fs');

// db.json이 있으면 읽고, 없으면 기본값 설정
let db;
if (fs.existsSync('./db.json')) {
    db = JSON.parse(fs.readFileSync('./db.json', 'utf8'));
} else {
    db = { lastCount: 0 };
}

// ... 이후 로직 진행
