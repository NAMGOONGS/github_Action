name: Site Monitor

on:
  schedule:
    - cron: '*/10 * * * *' # 10분마다 실행
  workflow_dispatch: # 수동 실행 버튼

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ github.token }}

      - name: Install dependencies
        run: npm install puppeteer

      - name: Run Scraper
        id: scraper
        run: |
          RESULT=$(node index.js)
          echo "DEBUG_LOG: $RESULT"
          
          if [[ "$RESULT" == *"NEW_DATA_DETECTED"* ]]; then
            # 'NEW_DATA_DETECTED' 이후의 텍스트만 추출하고, 줄바꿈을 공백으로 치환
            CLEAN_MSG=$(echo "$RESULT" | grep -A 10 "NEW_DATA_DETECTED" | sed '1d' | tr -d '\n' | tr -d '\r' | cut -c 1-150)
            echo "msg=$CLEAN_MSG" >> $GITHUB_OUTPUT
            echo "has_new=true" >> $GITHUB_OUTPUT
          else
            echo "has_new=false" >> $GITHUB_OUTPUT
          fi

      - name: Get Kakao Access Token
        run: |
          # 토큰을 받아오는 curl 명령어 실행 (기존 코드)
          RESPONSE=$(curl -X POST "https://kauth.kakao.com/oauth/token" ...)
          
          # JSON에서 access_token만 추출 (jq가 설치되어 있다면 jq 사용 권장)
          TOKEN=$(echo $RESPONSE | sed 's/.*"access_token":"\([^"]*\)".*/\1/')
          
          # ⭐ 중요: 이 줄이 있어야 다음 단계에서 ${{ env.ACCESS_TOKEN }}으로 쓸 수 있습니다.
          echo "ACCESS_TOKEN=$TOKEN" >> $GITHUB_ENV

      - name: Send Kakao Message
        if: steps.scraper.outputs.has_new == 'true'
        env:
          # 위 단계에서 저장한 토큰을 환경변수로 불러옵니다.
          KAKAO_TOKEN: ${{ env.ACCESS_TOKEN }}
        run: |
          curl -v -X POST "https://kapi.kakao.com/v2/api/talk/memo/default/send" \
            -H "Authorization: Bearer $KAKAO_TOKEN" \
            -d 'template_object={
                  "object_type": "text",
                  "text": "📌 새 공지 알림: '"$SAFE_MSG"'",
                  "link": {
                    "web_url": "https://excacademy.kr/rental-duty",
                    "mobile_web_url": "https://excacademy.kr/rental-duty"
                  },
                  "button_title": "확인하기"
                }'

      - name: Commit changes
        if: steps.scraper.outputs.has_new == 'true'
        run: |
          git config --global user.name 'bot'
          git config --global user.email 'bot@github.com'
          git add db.json
          git commit -m "Update db.json: $(date +'%Y-%m-%d %H:%M')" || exit 0
          git push
